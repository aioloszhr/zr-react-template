import React from 'react';

// import type { Menu } from './interface';

interface CreateMemuProps {
	visible: boolean;
	// onCancel: (flag?: boolean) => void;
	// curRecord?: Menu | null;
	// onSave: () => void;
	// editData?: Menu | null;
}

const CreateMenu: React.FC<CreateMemuProps> = () => {
	return <div>模版</div>;
};
export default CreateMenu;
